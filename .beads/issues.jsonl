{"_type":"issue","id":"forge-4or","title":"evals-ui/zinc.lock can silently desync from the root zinc.lock on `zinc update`","description":"manifest-evals/evals-ui is a dual-purpose member: it is a member of the forge root workspace AND its own standalone workspace for the wasm32-wasi build, carrying its own manifest-evals/evals-ui/zinc.lock. Shared deps (e.g. aeson 2.3.0.0) are duplicated between forge/zinc.lock and the evals-ui lock and currently match exactly. A future `zinc update` that bumps a shared dependency in the root lock will NOT touch the evals-ui lock, silently diverging the wasm build's dependency set until someone re-syncs it by hand. Pre-existing design (predates the monorepo migration); surfaced during the forge consolidation.","design":"Options: (a) add a CI/check step that asserts shared dep versions match between forge/zinc.lock and manifest-evals/evals-ui/zinc.lock; (b) restructure so evals-ui derives its wasm lock from the root lock instead of hand-maintaining a copy; (c) at minimum, document the sync requirement next to the evals-ui dual-purpose comment.","acceptance_criteria":"Drift between the two locks is either prevented or detected automatically, or the manual sync requirement is clearly documented.","status":"open","priority":2,"issue_type":"task","owner":"gstokes@superit.ai","created_at":"2026-06-16T03:19:57Z","created_by":"Gareth Stokes","updated_at":"2026-06-16T03:19:57Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"forge-6zs","title":"Root flake.nix sets no SSL_CERT_FILE; live HTTPS eval runs inside nix develop may fail TLS verification","description":"forge/flake.nix's devShell provides GHC + system libs but does not set SSL_CERT_FILE / include pkgs.cacert. crucible makes outbound HTTPS calls (e.g. the Anthropic API); manifest-evals eval runs that hit live endpoints from inside `nix develop` may fail certificate verification. None of the three original flakes set it, so this is NOT a regression from the migration — flagged for eval-run reliability.","design":"Add pkgs.cacert to the devShell packages and export SSL_CERT_FILE=${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt in the shellHook. Only needed if eval runs perform live HTTPS from within the dev shell.","acceptance_criteria":"A crucible HTTPS request succeeds from within `nix develop` without manual cert configuration.","status":"open","priority":2,"issue_type":"task","owner":"gstokes@superit.ai","created_at":"2026-06-16T03:19:57Z","created_by":"Gareth Stokes","updated_at":"2026-06-16T03:19:57Z","dependency_count":0,"dependent_count":0,"comment_count":0}
